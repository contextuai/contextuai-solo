# Simon Willison (@simonw) — Twitter/X Reply

**Target:** Reply to any SQLite or local AI tweet

---

Built ContextuAI Solo on top of SQLite — migrated from MongoDB by writing a compatibility layer that translates Motor API calls and MongoDB query operators ($set, $in, $regex) to SQL/JSON expressions. 93+ business agents running locally, all data in a single .db file on your machine. Your SQLite advocacy is what convinced me. Open source:

https://github.com/contextuai/contextuai-solo
